DOM?
The Document Object Model (Dom) is and application programming interface (API) for 
manipulating HTML and XML documents. (add remove, and modify parts of the document HTML.)

*** In the DOM, all the HTML elements are defined as objects so it will have both property and method.


One HTML element has only one ID ane class.




